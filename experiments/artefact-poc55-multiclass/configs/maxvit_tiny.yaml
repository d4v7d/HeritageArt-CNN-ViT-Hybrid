# MaxViT-Tiny Configuration for POC-5.5

_base_: poc55_256px.yaml

model:
  # Use a pretrained variant that exists in the installed timm (224px pretrained weights)
  encoder: maxvit_tiny_rw_224
  encoder_weights: sw_in1k

logging:
  experiment_name: poc55_maxvit_tiny_256px
  log_dir: ./logs/maxvit_tiny
